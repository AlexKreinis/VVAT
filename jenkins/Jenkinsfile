pipeline {
    agent {
        docker {
            image 'node' 
            args '-p 5000:5000' 
        }
    }
    stages {
        stage('build') {
            steps {
                sh 'cd Backend'
                sh 'npm install'
            }
        
        }
        
        stage('Test') {
            steps {
              sh 'cd Backend'
              sh 'npm run test'
            }
            // post {
            //     always {
            //         junit 'test-reports/results.xml'
            //     }
            // }
        }
    }
}