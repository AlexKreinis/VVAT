pipeline {
    agent {
        docker {
            image 'node' 
            args '-p 5000:5000' 
        }
    }
    stages {
        stage('build') {
            steps {
                sh 'echo start build'
                sh 'ls'
                sh 'cd Backend'
                sh 'ls'
                sh 'npm install'
            }
        
        }
        
        stage('Test') {
            steps {
              sh 'echo start test'
              sh 'ls'
              sh 'npm run test'
            }
            // post {
            //     always {
            //         junit 'test-reports/results.xml'
            //     }
            // }
        }
    }
}