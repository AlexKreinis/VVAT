pipeline {
    agent {
        docker {
            image 'node' 
            args '-p 5000:5000' 
        }
    }
    stages {
       
        stage('build') {
                steps { 
                    dir("Backend") {

                        sh 'npm install'
                    }
             }
        }
        
          stage('Test') {
                 steps {
                     dir("Backend") {
                      
                    
                    
                    sh 'npm run test'
                 }
            }
            // post {
            //     always {
            //         junit 'test-reports/results.xml'
            //     }
            // }
        }
    }
}