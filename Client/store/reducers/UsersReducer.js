const initialState = {};

const UsersReducers = (state = initialState, action) => {
  switch (action.type) {
    //complete cases
    case "":

    case "  ":

    default:
      return state;
  }
};

export default UsersReducers;
